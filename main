#!/bin/bash


set -e

# config.json から入力・出力を読み込む
t1=$(jq -r .t1 config.json)
outtxt=$(jq -r .header config.json)

echo "Input T1: $t1"
echo "Output dir: $outtxt"

# Run the processing
python main.py "$t1" "$outtxt" /output


