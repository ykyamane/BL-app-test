#!/bin/bash

#PBS -l nodes=1:ppn=1
#PBS -l vmem=4gb
#PBS -l walltime=00:05:00

set -e

# config.json
t1=$(jq -r .t1 config.json)

chmod +x main.py
chmod +x main

mkdir -p raw

singularity exec docker://brainlife/mne:1.2.1 python main.py "$t1" "raw/header.txt"



