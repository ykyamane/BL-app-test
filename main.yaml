name: Extract NIfTI Header
description: Read the header of a T1-weighted NIfTI image and save it as a text file.
author: Your Name
version: 1.0

inputs:
  - id: t1
    type: neuro/anat/t1w
    description: T1-weighted anatomical MRI image (NIfTI format)

outputs:
  - id: header
    type: raw
    description: Header information of the input NIfTI file

config:
  - id: bool_param
    type: bool
    description: test parameter

